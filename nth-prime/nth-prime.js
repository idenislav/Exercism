

export class Prime {
  nth(input) {

    let n = input; 
    const prime = true;

    for (var i = 2; i <= Math.sqrt(n); i++) {
      if (n % 2 == 0) {
        console.log("Prime is not possible");
        break;
      }

      if (prime) {
        console.log(" ")
      }
      else if 
        (console.log(" ")
      
    

   return nth;
  }
  
}
